package com.bubblego.app.android.ui.activities.data

import com.bubblego.app.android.ui.models.UiService

object DataProvider {

    val servicesList = listOf(
        UiService(
            0, false, "Exterior", "75"
        ),
        UiService(
            1, false, "Wheels", "20"
        ),
        UiService(
            2, false, "Back/Front Lights", "30"
        ),
    )

}